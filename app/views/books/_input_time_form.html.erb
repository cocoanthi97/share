<%= form_for @book, :url => {:action => 'create'} do |f| %>
1 <%= f.select :time1, @subs %><br>
2<%= f.select :time2, @subs %><br>
3<%= f.select :time3, @subs %><br>
4<%= f.select :time4, @subs %><br>
5<%= f.select :time5, @subs %><br>
<%= f.hidden_field :date, :value => @book.date %>
<%= f.submit "登録" %>

<% end %>